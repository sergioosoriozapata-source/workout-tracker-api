# Workout Tracker API — GA1-220501098-03-AA1-EV09

API RESTful con **Node.js + Express** (caso: usuarios, rutinas, ejercicios y progreso).

## Ejecución

```bash
npm install
npm run dev    # desarrollo (nodemon)
npm start      # producción
```

Base URL: `http://localhost:3000` · Versión API: `/api/v1`.

> Detalle de endpoints, request/response y estados: ver README en `develop`/`main` final.
